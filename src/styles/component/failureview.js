import { StyleSheet, Platform } from 'react-native';
import { colors } from '../misc/settings';

export const failureview = StyleSheet.create({
    container: {
        flex: 1,
        justifyContent: 'center',
        alignItems: 'center',
        backgroundColor: colors.gray200
    }
});